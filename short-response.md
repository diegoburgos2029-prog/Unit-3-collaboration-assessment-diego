# Short Response Question
1. If i am collaborating with a classmate the first thing we need to do is to create a branch.This is important that each member to do this step because they will each have their own work space not messing up the main.

2. Developers use brnaches to created space for new features,bugfix, or expenimental without messing with the main.

3. Git hub is a distributed version control system that every dev uses to acces all changes which they use to take a snapchat of what the code looks like at the momment.Github is a cloud based sevice that allows developers to store,track,mange and control changes to their code. These two work together by provideing us with a version control.

4. what is wrong with the following command is `git add .` should go first not `git commit -m "saving work"`. The correct way to do it is `git add .` `git commit -m "saving work"` `git push`.